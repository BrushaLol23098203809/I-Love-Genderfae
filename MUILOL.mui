import React from 'react';
import { TextField } from '@mui/material';

export default function FileUpload() {
  return (
    <TextField
      type="file"
      variant="outlined"
      slotProps={{
        input: {
          inputProps: { accept: 'image/*,application/pdf' } // Specify allowed file types
        }
      }}
    />
  );
}
